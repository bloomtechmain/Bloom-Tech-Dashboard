[phases.install]
cmds = ["npm install && npm install --prefix client && npm install --include=dev --prefix server"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node server/dist/index.js"
