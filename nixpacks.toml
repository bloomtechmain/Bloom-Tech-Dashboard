[phases.install]
cmds = ["npm install && npm install --include=dev --prefix client && npm install --include=dev --prefix server"]

[phases.build]
cmds = ["npm run build --prefix client"]

[start]
cmd = "./server/node_modules/.bin/tsx server/index.ts"
